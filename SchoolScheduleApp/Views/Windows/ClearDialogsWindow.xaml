<Window x:Class="SchoolScheduleApp.Views.Windows.ClearDialogsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="470"
        Height="185"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Border CornerRadius="12"
            Background="{DynamicResource WindowBackground}"
            BorderBrush="{DynamicResource CardBorder}"
            BorderThickness="1"
            Padding="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="TitleText"
                       Grid.Row="0"
                       FontSize="16"
                       FontWeight="Bold"
                       Foreground="{DynamicResource PrimaryText}"/>

            <TextBlock x:Name="DescriptionText"
                       Grid.Row="1"
                       Margin="0,8,0,0"
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource SecondaryText}"/>

            <Grid Grid.Row="2"
                  Margin="0,10,0,0"
                  HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button x:Name="CancelButton"
                        Grid.Column="0"
                        Margin="0,0,6,0"
                        MinWidth="110"
                        HorizontalAlignment="Stretch"
                        Click="Cancel_Click"/>
                <Button x:Name="DeleteForMeButton"
                        Grid.Column="1"
                        Margin="0,0,6,0"
                        MinWidth="130"
                        HorizontalAlignment="Stretch"
                        Background="#475569"
                        Foreground="White"
                        BorderThickness="0"
                        Click="DeleteForMe_Click"/>
                <Button x:Name="DeleteForAllButton"
                        Grid.Column="2"
                        MinWidth="130"
                        HorizontalAlignment="Stretch"
                        Background="#b91c1c"
                        Foreground="White"
                        BorderThickness="0"
                        Click="DeleteForAll_Click"/>
            </Grid>
        </Grid>
    </Border>
</Window>
